<!DOCTYPE html>
<html lang="en">
<head>
  <% include partials/_head %>
  <title>TinyApp</title>
</head>
<body class>
  <% include partials/_header %>
  <div class="container">
      <h2> List of URLs </h2>
      <ul class="list-group">
      <% for (url in urls) { %>
        <li class="list-group-item clearfix">
          <a href="<%= urls[url].long %>"><%= urls[url].long %></a>  ⟶  <%= url %>
          <% if (userID === urls[url].userID) { %>
          <div class="btn-group pull-right">
            <form class="form-inline" method="POST" action="/urls/<%= url %>/delete"> 
            <a class="btn btn-default" href="/urls/<%= url %>">Edit</a>
              <button onclick="submit" class="btn btn-danger">Delete</button>
            </form>
          </div>
          <% } %>
        </li>
      <% } %>
      </ul>
  </div>
</body>
</html>