<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <h2> List of URLs </h2>
  <ul>
  <% for (key in urls) { %>
    <li><strong><%= urls[key] %></strong> is shortened to <b><%= key %></b></li>
  <% } %>
  </ul>
</body>
</html>