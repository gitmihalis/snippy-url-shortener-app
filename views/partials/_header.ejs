<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <img src="https://github.com/gitmihalis/snippy-url-shortener-app/blob/master/docs/hand_scissors.png?raw=true" alt="hand scrissors">
      <% if ( !user ) { %>
        <% include _signup_links %>
      <% } else { %>
        <a class="btn btn-default navbar-btn" href="/logout">Logout</a>
      <% } %>
    </div>
  </div>
</nav>
<div class="container">
  <div class="jumbotron">
    <h1>Snippy App</h1> 
    <% if ( user ) { %>
      <h4> Hello, <%= user.email %>
    <% } %>
    <p>Snippy is the most popular URL link shortener in the entire LHL community right now.</p> 
  </div>
</div>